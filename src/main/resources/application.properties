# Server
server.port=8080
spring.application.name=db_hrms

# DataSource ??c t? env khi ch?y Docker, fallback local khi dev
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/db_hrms}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123456}

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# -----------------------------
# Keycloak / OAuth2 (DISABLED)
# -----------------------------
# The following OAuth2 / OIDC properties are intentionally commented out
# because Keycloak is not expected to run during local development. If you
# run a Keycloak server and want to enable OIDC login/resource-server
# behavior, uncomment these lines and ensure the issuer URL is reachable.

# Keycloak OAuth2 (provider / registration)
# spring.security.oauth2.client.provider.keycloak.issuer-uri=\
#   ${SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_ISSUER_URI:\
#   http://localhost:8180/realms/hrms}

# spring.security.oauth2.client.registration.keycloak.client-id=\
#   ${SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_ID:hrms-client}

# spring.security.oauth2.client.registration.keycloak.client-secret=\
#   ${SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET:YOUR_CLIENT_SECRET}

# spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code
# spring.security.oauth2.client.registration.keycloak.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
# spring.security.oauth2.client.registration.keycloak.scope=openid,profile,email

# Resource server JWT issuer (if using Keycloak as resource server)
# spring.security.oauth2.resourceserver.jwt.issuer-uri=\
#   ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI:\
#   http://localhost:8180/realms/hrms}

# If you want to completely disable the OAuth2 client auto-configuration
# (so Spring Boot will not create OAuth2 client beans when no provider is
# configured or the issuer is unreachable), uncomment the following line.
# This is useful during local dev when Keycloak is not running.
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.client.OAuth2ClientAutoConfiguration

# -----------------------------
# App & Keycloak endpoints
# -----------------------------
app.base-url=${APP_BASE_URL:http://localhost:8081}
# These keycloak.* URLs are only required if you use Keycloak; keep them
# commented if Keycloak is disabled. They are left here for reference.
# keycloak.logout-url=${KEYCLOAK_LOGOUT_URL:http://localhost:8180/realms/hrms/protocol/openid-connect/logout}
# keycloak.login-url=${KEYCLOAK_LOGIN_URL:http://localhost:8180/realms/hrms/protocol/openid-connect/auth}
# keycloak.client-id=${KEYCLOAK_CLIENT_ID:hrms-client}

# Keycloak Admin (optional helper). If you don't run Keycloak, you can
# comment out these admin properties as well to avoid accidental remote calls.
# keycloak.admin.server-url=${KEYCLOAK_ADMIN_SERVER_URL:http://localhost:8180}
# keycloak.admin.realm=${KEYCLOAK_ADMIN_REALM:hrms}
# keycloak.admin.client-id=${KEYCLOAK_ADMIN_CLIENT_ID:admin-cli}
# keycloak.admin.username=${KEYCLOAK_ADMIN_USERNAME:admin}
# keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD:admin}
